<h1>Welcome to Brandy Gras!</h1>

<%= image_tag 'EyeOnly.png', size: "400x410" %>
<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @mardis.each do |mardi| %>
      <tr>
        <td><%= link_to 'Show', mardi %></td>
        <td><%= link_to 'Edit', edit_mardi_path(mardi) %></td>
        <td><%= link_to 'Destroy', mardi, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Mardi', new_mardi_path %>
